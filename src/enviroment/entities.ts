import { User } from 'src/auth/user.entity';
import { Project } from 'src/project/project.entity';
import { Update } from 'src/update/update.entity';

export const ENTITIES = [Project, User, Update];
